# DESIGN_DISCUSSION.md

CLAUDE.md의 "설계 진행 방식"(사용자가 먼저 생각을 말하고, Claude는 맞으면 맞다고 틀리면 왜 틀렸는지 설명하는 방식)을 따라 진행한 설계 논의 기록.
아직 CLAUDE.md의 "아키텍처 결정사항" 표에 반영되지 않은, 진행 중이거나 새로 도출된 내용만 담는다.
여기 내용이 확정되면 CLAUDE.md의 아키텍처 결정사항 표로 옮긴다.

## 1.1 랜덤 문제 조회 — 문제 선택 책임

### 논의된 원칙

- 데이터를 가진 객체가 그 데이터에 대한 판단도 스스로 한다. 외부 객체가 getter로 데이터를 꺼내 if문으로 판단하면 책임이 잘못된 위치에 있다는 신호다.
- `Problem` 하나는 자기 자신에 대한 정보(챕터, 지문, 선택지, 정답)만 알 수 있다. "이 사용자가 나를 이미 풀었는가", "여러 문제 중 무엇이 남았는가"는 개별 `Problem`이 알 수 없는 정보이며, 이는 "여러 개를 다루고 비교/선택하는" 책임이라 개별 `Problem`의 책임 범위를 벗어난다.

### 도출된 결론

- "사용자가 안 푼 문제 중 랜덤으로 하나 고르기"는 `Problem` 단일 객체가 아니라 별도의 협력 객체(가칭 `RandomProblemSelector`, 최종 이름/위치는 기존 아키텍처의 domain/service 패키지 컨벤션에 맞춰 결정)가 담당해야 한다.

### 미결 사항 (다음에 이어서 논의)

이 selector가 아래 두 정보를 다룰 때, 직접 소유/조회할지 아니면 다른 객체에게 물어봐서 받아올지 아직 결론 내지 않음.

- 이 사용자가 이미 푼 문제 목록 (풀이 이력을 가진 객체의 책임일 가능성이 높음 — `UserProblemHistory` 쪽과 연계 검토 필요)
- 직전에 건너뛴 문제 (이미 `UserChapterSkip` 엔티티가 아키텍처 결정사항에 존재하므로, selector는 이 정보를 `UserChapterSkip` 쪽에 물어보는 방식이 원칙에 맞을 가능성이 높음 — 확정 전 논의 필요)

원칙상으로는 "데이터를 가진 객체에게 책임을 준다"는 기준에 따라, selector가 직접 풀이 이력/건너뛰기 데이터를 들고 있기보다는 `UserProblemHistory`, `UserChapterSkip` 등 기존에 확정된 엔티티에게 물어보는 구조로 가는 것이 자연스러워 보이나, 사용자 확인 후 CLAUDE.md에 반영할 것.

## 요구사항 → 책임 분해 절차 (참고용, 향후 다른 기능에도 동일하게 적용)

1. 요구사항 문장에서 동사를 모두 뽑는다.
2. 각 동사(책임 후보)를 "그 데이터를 가진 객체"에게 우선 배정한다.
3. 한 객체가 감당할 수 없는 일(여러 개를 다루거나 비교/선택)은 새 협력 객체를 만든다.
4. 같은 책임을 수행하는 방법이 여러 갈래일 수 있으면(판정 방식, 선택 전략 등) 인터페이스로 분리할지 검토한다.